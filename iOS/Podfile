# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'FacebookSearch' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FacebookSearch
  pod 'Alamofire', '~> 4.4'
  pod 'SwiftyJSON', '~> 3.1'
  pod 'SwiftSpinner', '~> 1.2'
  pod 'EasyToast', '~> 2.0'
  pod 'FacebookCore'
  pod 'FacebookLogin'
  pod 'FacebookShare'

end
